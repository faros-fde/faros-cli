# =============================================================================
# Faros CLI Credentials
# =============================================================================
# All API keys and tokens MUST be set here (or as environment variables).
# Never put credentials in faros.config.yaml.
# Copy this file to .env and fill in your values.
# =============================================================================

# Faros API key (required for all sync commands)
FAROS_API_KEY=your_faros_api_key_here

# -----------------------------------------------------------------------------
# Data Source Credentials
# Configure only the sources you use
# -----------------------------------------------------------------------------

# Linear (required for: faros sync linear)
# Generate at: Linear Settings > API > Personal API keys
# LINEAR_API_KEY=lin_api_xxxxx

# GitHub (required for: faros sync github)
# GITHUB_TOKEN=ghp_xxxxx

# AWS Credentials (for S3 test results)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# AWS_PROFILE=default

# -----------------------------------------------------------------------------
# Optional Overrides
# These can also be set in faros.config.yaml
# -----------------------------------------------------------------------------

# FAROS_URL=https://prod.api.faros.ai
# FAROS_GRAPH=default
# FAROS_ORIGIN=faros-cli

# CI/CD Detection (automatically detected, override if needed)
# CI=true
