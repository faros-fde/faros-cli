node_modules/
/lib/
*.log
.DS_Store
dist/
coverage/

# Credentials - never commit!
.env
.env.local
.env.*.local
.npmrc

# Config files (may contain sensitive data)
faros.config.json
.farosrc.yaml
.farosrc.yml
.farosrc.json

# AWS credentials
.aws/
test-results/

# Temporary sync files (may contain secrets)
faros_airbyte_cli_config.json
state.json
